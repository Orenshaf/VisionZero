const ChooseData = (data) => {
    
    // const intermediate = Object.keys(data.BRONX).map(year => {
    //     return { year: year, deaths: data.BRONX[year].deaths, injuries: Math.round(data.BRONX[year].injuries / 100) };
    // })

    // const series = d3.stack()
    //     .keys(["deaths", "injuries"])(intermediate);
}

export default ChooseData;